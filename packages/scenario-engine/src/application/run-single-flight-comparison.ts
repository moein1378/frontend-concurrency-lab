import { DeterministicClock, EventLog } from '@concurrency-lab/timeline'; import type { SingleFlightComparison, SingleFlightRun } from '../domain/single-flight'
export interface SingleFlightOptions { seed: string; resourceId: string; subscribers: number; cancelSubscriber?: number; failFirst?: boolean; staleWhileRefresh?: boolean }
function run(variant: 'broken' | 'fixed', options: SingleFlightOptions): SingleFlightRun { const clock = new DeterministicClock(); const log = new EventLog(clock); const key = `profile:${options.resourceId.trim().toLowerCase()}`; const cancelled = options.cancelSubscriber && options.cancelSubscriber <= options.subscribers ? 1 : 0; for (let i=1;i<=options.subscribers;i++) log.record('request', `Subscriber ${i} requested ${key}`, { subscriber:i,key }); const producerCalls = variant === 'broken' ? options.subscribers : 1; if (variant === 'fixed') for(let i=2;i<=options.subscribers;i++) log.record('queued', `Subscriber ${i} joined the in-flight promise`, { subscriber:i,key }); if (options.cancelSubscriber) log.record('cancel', `Subscriber ${options.cancelSubscriber} stopped listening; producer ownership stayed shared`, { subscriber:options.cancelSubscriber,key }); const staleShown = Boolean(options.staleWhileRefresh); if (staleShown) log.record('commit','Cached value shown immediately while one refresh runs',{key,stale:true}); clock.advanceBy(180); if(options.failFirst) log.record('fail','Shared producer rejected; registry entry removed for retry',{key}); log.record('response',`${producerCalls} producer call${producerCalls===1?'':'s'} completed`,{key,producerCalls}); const delivered=options.subscribers-cancelled; log.record('commit',`Fresh result fanned out to ${delivered} active subscribers`,{key,delivered}); const passed=producerCalls===1; log.record('invariant',passed?'One producer served every active subscriber':'Duplicate callers started duplicate producers',{key,producerCalls,delivered}); return {variant,producerCalls,subscribers:options.subscribers,delivered,staleShown,invariant:{passed,statement:'At most one producer may run per normalized in-flight key; subscriber cancellation does not cancel shared ownership.'},events:log.all()} }
export function runSingleFlightComparison(options: SingleFlightOptions): SingleFlightComparison { if(options.subscribers<2) throw new RangeError('At least two subscribers are required'); const key=`profile:${options.resourceId.trim().toLowerCase()}`; return {key,broken:run('broken',options),fixed:run('fixed',options)} }
