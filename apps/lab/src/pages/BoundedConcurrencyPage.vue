<script setup lang="ts">import { useI18n } from 'vue-i18n'; import BoundedConcurrencyLab from '../modules/bounded-concurrency/BoundedConcurrencyLab.vue'; const { locale } = useI18n(); const backUrl = `${import.meta.env.BASE_URL}scenarios`</script>
<template><article><section class="detail-hero"><a class="back-link" :href="backUrl">{{ locale === 'fa' ? 'بازگشت به سناریوها →' : '← Back to scenarios' }}</a><p class="eyebrow">{{ locale === 'fa' ? 'سناریوی ۰۳ · هم‌زمانی محدود' : 'Scenario 03 · Bounded concurrency' }}</p><h1>{{ locale === 'fa' ? 'سرعت بارگذاری را بدون اشباع منابع کنترل کنید' : 'Control upload throughput without resource saturation' }}</h1><p>{{ locale === 'fa' ? 'شروع هم‌زمان همهٔ کارها ظرفیت مرورگر و سرور را نادیده می‌گیرد. semaphore تعداد permitها را محدود و بقیهٔ کارها را منصفانه صف‌بندی می‌کند.' : 'Starting every job at once ignores browser and server capacity. A semaphore limits active permits and queues the rest fairly.' }}</p><div class="teaching-callouts"><article><h2>{{ locale === 'fa' ? 'مدل ذهنی' : 'Mental model' }}</h2><p>{{ locale === 'fa' ? 'ظرفیت تعداد صندلی‌هاست؛ صف ترتیب انتظار است و release صندلی را حتی پس از خطا یا لغو برمی‌گرداند.' : 'Capacity is the number of seats, the queue preserves arrival order, and release returns a seat even after failure or cancellation.' }}</p></article><article><h2>{{ locale === 'fa' ? 'هزینهٔ انتخاب' : 'Trade-off' }}</h2><p>{{ locale === 'fa' ? 'ظرفیت بیشتر ممکن است throughput را بالا ببرد اما فشار حافظه، شبکه و rate-limit را نیز زیاد می‌کند.' : 'Higher capacity may improve throughput, but increases memory, network pressure, and rate-limit risk.' }}</p></article></div></section><BoundedConcurrencyLab /></article></template>
