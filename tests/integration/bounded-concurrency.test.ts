import { describe, expect, it } from 'vitest'; import { runUploadComparison } from '@concurrency-lab/scenario-engine'; describe('phase 4 uploads', () => { it('shares controls and preserves capacity', () => { const c = runUploadComparison({ seed: 'review', jobs: 7, capacity: 3 }); expect(c.broken.capacity).toBe(c.fixed.capacity); expect(c.fixed.peakActive).toBeLessThanOrEqual(3); expect(c.fixed.completed).toBe(7) }) })
