import{expect,test}from'@playwright/test';test('reviewer distinguishes single-flight from caching',async({page})=>{await page.goto('/scenario/single-flight/compare');await page.getByRole('button',{name:'Run comparison'}).click();await expect(page.getByText('Duplicate producers',{exact:true})).toBeVisible();await expect(page.getByText('One shared producer',{exact:true})).toBeVisible();await expect(page.getByText('Normalized key:')).toBeVisible()})
