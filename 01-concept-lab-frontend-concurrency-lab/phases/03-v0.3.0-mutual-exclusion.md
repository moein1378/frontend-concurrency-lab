# Phase 03 — v0.3.0 — Mutual Exclusion

## Release objective

Implement mutex and double-submit protection.

## User-visible outcome

At the end of this phase a reviewer can use a coherent vertical slice rather than isolated utilities. The visible surface is limited to: Scenario Detail, Broken vs Fixed Comparison, Event Timeline.

## In-scope deliverables

- Implement mutex and double-submit protection.
- Add payment-like and save-form scenarios.
- Prove lock release on success, failure, timeout, and cancellation.

## Modules and directories activated in this phase

- `single-flight registry` — implement or extend the single flight registry responsibility.
- `mutex` — implement or extend the mutex responsibility.
- `semaphore` — implement or extend the semaphore responsibility.
- `abort propagation` — implement or extend the abort propagation responsibility.

## Planned files and ownership

| Planned path | Responsibility in this phase |
|---|---|
| `apps/lab/src/modules/<phase-03>/` | UI/application composition for this phase; replace placeholder with the owned feature name. |
| `packages/test-fixtures/src/` | Implement the phase-owned capability inside packages/test-fixtures. |
| `packages/timeline/src/` | Implement the phase-owned capability inside packages/timeline. |
| `packages/scenario-engine/src/` | Implement the phase-owned capability inside packages/scenario-engine. |
| `apps/lab/src/pages/` | Add or update only the routes/surfaces named in this phase. |
| `tests/e2e/` | Add the phase reviewer journey and failure path. |
| `docs/adr/` | Record any material architecture decision. |
| `docs/runbooks/` | Update startup/deployment/reset instructions when behavior changes. |

## Technology introduced or unlocked

- `Vue 3` — introduced only for the phase responsibilities above.
- `Vite` — introduced only for the phase responsibilities above.
- `TypeScript` — introduced only for the phase responsibilities above.
- `Tailwind CSS 4; custom lab components; no SSR` — introduced only for the phase responsibilities above.

## Local execution contract

1. `corepack enable`
2. `pnpm install --frozen-lockfile`
3. `pnpm seed` to restore deterministic assets and fixtures.
4. `pnpm dev` for the primary experience or `pnpm dev:all` only when this phase activates services.
5. `pnpm verify` before the phase may be tagged.

The phase must remain usable in fixture/local mode even when an optional hosted provider is unavailable.

## Live-demo state

Deploy an internal/branch artifact or the first fixture demo; the primary workflow must remain deterministic.

## CI/CD changes required in this phase

- Extend the CI matrix only for capabilities introduced in this phase.
- Add unit/integration tests for every new invariant.
- Add a production build and verify generated artifacts are reproducible.
- Run documentation links and secret scanning.
- Do not deploy if `verify` fails.

## Docker and service state

Not required. A tiny optional Hono latency fixture may run directly with Node; Docker is intentionally avoided for the primary workflow.

For this phase, containers are added only if a newly activated service cannot be represented reliably by a direct local command.

## Responsive and accessibility acceptance

- Apply the project decision: Adaptive desktop-first lab; usable on tablet, with phone mode limited to single-column experiment playback.
- New interactions must be keyboard reachable and expose visible focus.
- Touch targets are at least 44px where the phase includes mobile interaction.
- State, failure, and progress are never communicated by color alone.
- `prefers-reduced-motion` behavior is tested when motion appears.

## Test plan

- Unit tests for domain transitions and pure policies.
- Integration tests across the activated modules.
- At least one deterministic browser workflow covering the user-visible outcome.
- Failure-path tests for cancellation, retry, stale data, invalid inputs, or unavailable capabilities relevant to this phase.
- Snapshot/visual tests only for stable visual contracts, not dynamic telemetry.

## Documentation and brand work

- Update README progress/status and phase badge.
- Add or update an ADR for any material decision.
- Add screenshots or a short GIF only after the workflow is stable.
- Use generated brand assets from `brand/`; do not redraw logos.
- Update `docs/toolchain.lock.md` when exact versions change.

## Exit criteria

- [ ] Every in-scope item is implemented without placeholder success states.
- [ ] `pnpm verify` passes on a clean clone.
- [ ] Fixture mode and documented local mode both work.
- [ ] The new user-visible workflow has an automated browser test.
- [ ] No paid service is required.
- [ ] Architecture boundaries pass.
- [ ] Documentation describes known limitations and recovery paths.
- [ ] A Git tag `v0.3.0` and matching GitHub Release can be created.

## AI-agent execution note

An agent must implement only the tasks in this file. Any scope expansion is proposed in `agent/TASK-BRIEF.md` and accepted through an ADR before code changes.
