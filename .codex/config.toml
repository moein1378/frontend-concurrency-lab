# Project-scoped Codex configuration for the agent kit.
# Model selection intentionally inherits from the user's/runtime configuration.
[agents]
enabled = true
max_concurrent_threads_per_session = 6
interrupt_message = true
